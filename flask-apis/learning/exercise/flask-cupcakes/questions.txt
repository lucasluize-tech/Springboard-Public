1 - For some reason when I create the variable deleteButton = $('.delete')
    The browser does not recognize the buttons. But inside the console if $('.delete')
    is typed, then works normally, and we can even add the functionality flawlessly. 